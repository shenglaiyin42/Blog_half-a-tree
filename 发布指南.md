# 半棵斋文章发布指南

[`文章模板.md`](文章模板.md) 是本项目当前唯一有效的文章模板。新文章必须直接复制这份模板，不再使用或维护以前的 YAML 模板。每篇文章只需提供这一份 Markdown 文件。文件顶部的“文章信息”属于可编辑内容，直接修改每一项冒号后面的文字即可；发布时这一区不会显示在网页正文中。

## 必填信息

- `标题`：文章标题。
- `网址名`：公开网址使用的英文名称，只能填写小写英文字母、数字和连字符；起草时可以留空，由我在预览前补全。
- `首次发表`：`YYYY-MM-DD` 格式的首次发布日期。文章发布后保持不变。
- `更新于`：可选。首次发布时留空；文章以后发生实质修改时，再填写 `YYYY-MM-DD` 格式的最近一次修订日期。
- `摘要`：约 100 字的列表页摘要。
- `话题`：多个话题用中文逗号隔开，不写 `#`。Writing、Topics 和 Archive 会据此自动更新。
- `名片图片`：可选。只在正文有多张图片时填写，用来指定文章名片和朋友圈海报使用哪一张。

## 发布约定

- 正文从“文章信息”区结束后开始；不要重复写一级标题。
- “网址名”起草时可留空，预览前由我补充。
- 每篇文章首次发布时只显示“发表于”。只有“更新于”的日期晚于“首次发表”时，页面才会显示“更新于”；以后修订时保留首次发表日期不变，只填写或更新“更新于”。
- 原始 Markdown 会原样保存在本地和仓库的内容目录中。
- 每篇文章都统一进入 Writing，不再要求选择“文章”或“艺文”栏目。
- 页面会自动使用当前的标题、日期、话题、预览、全文排版及分享按钮；同时自动更新首页、Archive、Statistics、链接分享卡片与朋友圈文章海报（含文章二维码）。
- 历史文章中的 `section` 与 `tags` 字段继续兼容，不需要批量修改；新文章统一使用“话题”。
- 图片规则：正文没有图片时，名片使用博客顶图；正文只有一张图片时，名片自动使用该图；正文有多张图片时，发布会暂停并提示填写 `share_image`，以免擅自替你选择。名片中的图片始终等比完整显示，不裁切、不拉伸。

## 修改已发布文章

请修改 `content/posts/` 中相应的 Markdown 原稿，同时把“更新于”改为本次修订日期，然后重新发布。不要修改原来的“首次发表”，也不要把 `articles/` 中的 HTML 页面作为长期修改来源，否则下次重新发布时，HTML 中的改动会被覆盖。
