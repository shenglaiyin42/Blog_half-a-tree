# 半棵斋文章发布指南

请复制 [`文章模板.md`](文章模板.md) 并填写。每篇文章只需提供这一份 Markdown 文件。

## 必填信息

- `title`：文章标题。
- `section`：`essays`（文章）或 `arts`（艺文）。
- `date`：`YYYY-MM-DD` 格式的发布日期。
- `updated`：可选，文章最近一次修改日期；填写后页面会显示“更新于”。
- `summary`：约 100 字的列表页摘要。
- `tags`：标签名称列表；不写 `#`。
- `share_image`：可选。只在正文有多张图片时填写，用来指定文章名片和朋友圈海报使用哪一张。

## 发布约定

- 正文从 YAML 分隔线后的第一行开始；不要重复写一级标题。
- `slug` 可留空，发布时自动生成公开网址名。
- 原始 Markdown 会原样保存在本地和仓库的内容目录中。
- 页面会自动使用当前的标题、日期、栏目、标签、预览、全文排版及分享按钮；同时自动生成链接分享卡片与朋友圈文章海报（含文章二维码）。
- 图片规则：正文没有图片时，名片使用博客顶图；正文只有一张图片时，名片自动使用该图；正文有多张图片时，发布会暂停并提示填写 `share_image`，以免擅自替你选择。名片中的图片始终等比完整显示，不裁切、不拉伸。

## 修改已发布文章

请修改 `content/posts/` 中相应的 Markdown 原稿，然后重新发布。不要把 `articles/` 中的 HTML 页面作为长期修改来源，否则下次重新发布时，HTML 中的改动会被覆盖。
