# Blog Project Instructions

## 唯一文章模板

- 本项目唯一有效的文章模板是根目录的 `文章模板.md`。
- 每次创建新文章前，都必须先读取并直接套用这份模板；不要自行重建模板，也不要使用旧版 YAML 模板。
- 新文章保存在 `content/posts/`，顶部保留模板中的可编辑“文章信息”区。
- 发布程序对 YAML frontmatter 的支持仅用于兼容历史文章，不代表存在第二份模板。
- 如果以后调整模板，需要同时更新 `文章模板.md`、`发布指南.md` 和 `scripts/publish_post.py`，确保本地文档与 GitHub 保持一致。
